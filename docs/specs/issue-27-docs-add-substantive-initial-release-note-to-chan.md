---
issue_number: 27
issue_title: "docs: add substantive initial release note to CHANGELOG"
repo: "beogip/code-first-agents-tool"
labels: [documentation, P2]
plan_level: "lean"
depth: "medium"
branch_name: "docs/27-changelog-initial-release-note"
created_at: "2026-06-07T23:48:25Z"
---

# Implementation Plan: #27 — docs: add substantive initial release note to CHANGELOG

> **Decisions locked with user (grill-me discovery):**
> - **Placement:** BOTH — append a hand-written `## [0.1.0]` section at the **bottom** of `CHANGELOG.md` **and** create a GitHub Release v0.1.0.
> - **Style:** Hybrid — keep the Angular heading + `### Features` structure, but bullets describe the product in human language (no commit hashes).

## Files

| Action | Path | Purpose |
|--------|------|---------|
| modify | `CHANGELOG.md` | Append a `## [0.1.0] (2026-05-24)` section at the **bottom** with a hybrid, human-readable `### Features` list |
| create | GitHub Release `v0.1.0` (not a committed file) | Created via `gh release create v0.1.0 --notes-file …` with rich human-readable notes |

## Codebase Context

- `CHANGELOG.md` is owned/generated by `@semantic-release/changelog`; `@semantic-release/git` commits it on every release (`.releaserc.json`). semantic-release **prepends** new entries at the **top** and **never regenerates an existing tag** → appending v0.1.0 at the bottom is safe from future automated releases.
- v0.1.0 is currently **absent** from `CHANGELOG.md` (the file starts at v0.1.1). The v0.1.0 tag exists (commit `7ef0a4e`, dated 2026-05-24).
- **No GitHub Release** exists for v0.1.0 (earliest release is v0.1.1) → it must be **created**, not edited.
- v0.1.0 has no prior tag, so a `compare` link is not valid; the heading links to `commits/v0.1.0`.
- Real public API to describe accurately (from `src/`): `Tool` class (`.subcommand` / `.run` / `.invoke`), `l1Output` / `l2Output` / `l3Output`, `ToolError`, auto-registered `schema` + `help` builtins, always-exit-0 semantics, Zod v4 peer dependency, Bun >= 1.0 runtime.
- `CONTRIBUTING.md` documents no manual-CHANGELOG policy; entries are normally commit-driven via Conventional Commits. The v0.1.0 narrative entry is an intentional, one-off human exception.

## Steps

1. **Append the hybrid `## [0.1.0]` section to the bottom of `CHANGELOG.md`.**
   **Done when:** the file contains a `## [0.1.0]` heading dated `2026-05-24` followed by a `### Features` list of ≥5 human-readable bullets covering the `Tool` class, L1/L2/L3 output levels, schema/help introspection, always-exit-0 semantics, and Zod v4; and the existing v0.1.1–v0.1.4 entries are unchanged.

2. **Verify file hygiene after the edit.**
   **Done when:** `git diff --check` returns nothing (no trailing-whitespace / EOL errors) and the file ends with exactly one trailing `\n`.

3. **Verify no regression of prior entries.**
   **Done when:** `git diff -- CHANGELOG.md` shows only **added** lines below the v0.1.1 block (no `-` lines other than diff headers), satisfying AC-4.

4. **Create the GitHub Release v0.1.0 using a notes file.**
   Precheck with `gh release view v0.1.0`; write notes to a temp file (HEREDOC) and pass `--notes-file` to avoid shell-quoting issues with backticks/code blocks. Fall back to `gh release create --draft` if create fails due to tag state.
   **Done when:** `gh release view v0.1.0` returns the release with substantive human-readable notes.

## Interfaces

N/A — docs/release-only change; no code interfaces exchanged.

## Function Design

N/A — no code functions introduced or modified.

## Acceptance Criteria (EARS)

- **AC-1.** The `CHANGELOG.md` shall contain a `## [0.1.0]` section describing the initial release.
- **AC-2.** When a reader views the v0.1.0 section, it shall describe the `Tool` base class, Zod input/output schemas, L1/L2/L3 output levels, schema/help introspection, and always-exit-0 semantics.
- **AC-3.** The v0.1.0 section shall be appended at the bottom of `CHANGELOG.md` so that semantic-release does not overwrite it.
- **AC-4.** The existing v0.1.1 through v0.1.4 entries shall remain byte-for-byte unchanged.
- **AC-5.** When the maintainer publishes the release, a GitHub Release for tag v0.1.0 shall exist with human-readable notes.
- **AC-6.** The `## [0.1.0]` heading and `### Features` sub-heading shall match the Angular-style format of existing entries (date format `YYYY-MM-DD`, bracketed-link heading pattern).

## Out of Scope

- Modifying the semantic-release config or release automation.
- Backfilling CHANGELOG entries for any version other than v0.1.0.
- Rewriting existing machine-generated entries into prose.

## Edge Cases + Error Handling

| # | Scenario | Source | Handling |
|---|----------|--------|----------|
| 1 | semantic-release prepends future entries at the top of the file | [inferred] | Place v0.1.0 at the bottom; it is never regenerated for an existing tag |
| 2 | v0.1.0 has no prior tag for a `compare` link | [inferred] | Link the heading to `commits/v0.1.0` instead of a compare URL |
| 3 | GitHub Release v0.1.0 does not exist yet | [inferred] | Use `gh release create` (not `edit`) |
| 4 | Release notes contain backticks / Markdown code blocks | [from plan-critic] | Write notes to a temp file and pass `--notes-file` (HEREDOC); never inline `--notes` |
| 5 | Trailing whitespace / line-ending dirties the working tree before next auto-release | [from plan-critic] | Run `git diff --check` and confirm a single trailing newline after editing |
| 6 | Tag v0.1.0 state mismatch (lightweight vs annotated) or a draft release already exists | [from plan-critic] | Run `gh release view v0.1.0` first; fall back to `gh release create --draft` and promote manually |

## Done Criteria per Feature

| Feature | Done when |
|---------|-----------|
| CHANGELOG v0.1.0 note | AC-1, AC-2, AC-3, AC-4, AC-6 |
| GitHub Release v0.1.0 | AC-5 |

## Risks

- **semantic-release reformats CHANGELOG on the next release** → mitigated by bottom placement (the prepend region is the top; existing tags are never regenerated).
- **Inaccurate feature description** → mitigated by sourcing the text from actual `src/` exports via context-collector, not from memory.
- **GitHub Release step requires `gh` auth + push rights, and the tag state may differ** → precheck with `gh release view`, fall back to `--draft`; the required CHANGELOG task completes independently of the optional release.

## Test Strategy

- **AC-4 (no regression):** `git diff -- CHANGELOG.md` shows only additions below the v0.1.1 block (no removed lines except diff headers).
- **Hygiene:** `git diff --check` reports no whitespace errors; file ends in a single newline.
- **AC-5 (release exists):** `gh release view v0.1.0` confirms the release with substantive notes.
- No automated tests — docs/release-only change with no code paths to exercise.

---

## Proposed CHANGELOG v0.1.0 entry (reference content)

```markdown
## [0.1.0](https://github.com/beogip/code-first-agents-tool/commits/v0.1.0) (2026-05-24)

Initial release of `@code-first-agents/tool` — the TypeScript implementation of the
[Code-First Agents](https://code-first-agents.com/patterns/deterministic-tools.html) pattern. It
provides the `Tool` base class for building deterministic CLI tools that LLM agents can discover,
validate, and invoke with zero guesswork.

### Features

* **`Tool` base class** — register subcommands with Zod input/output schemas, then dispatch from the
  CLI via `.run(argv)` (always exits 0) or programmatically via `.invoke(name, args)`.
* **Typed output levels (L1/L2/L3)** — `l1Output` (raw data), `l2Output` (classification), and
  `l3Output` (instructions) helpers that bake the `{ ok, message }` envelope for the output spectrum.
* **Self-describing introspection** — auto-registered `schema` (JSON Schema for every subcommand) and
  `help` builtins; neither is user-overridable.
* **Always-exit-0 semantics** — every path, including errors, exits 0 and reports status via an
  `ok: false` JSON envelope (`unknown_subcommand`, `input_validation_error`, `schema_violation`,
  `non_object_return`, `unexpected_error`); handlers throw `ToolError` for domain-specific codes.
* **Zod v4 schema validation** — input/output validated on every call; `.strict()` input schemas
  reject unknown flags. Requires the `zod@^4` peer dependency; runs on Bun >= 1.0.
```
